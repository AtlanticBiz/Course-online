# Academy Platform Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB (in production, replace with actual MongoDB URI)
MONGO_URI=mongodb://localhost:27017/academy_platform

# JWT Secret (in production, use a strong random string)
JWT_SECRET=academy_platform_secret_key_for_development

# Stripe API Keys (in production, use actual Stripe keys)
STRIPE_PUBLIC_KEY=pk_test_mock_key
STRIPE_SECRET_KEY=sk_test_mock_key
